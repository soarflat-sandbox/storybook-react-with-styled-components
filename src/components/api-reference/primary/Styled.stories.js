import React from 'react';

import { Styled } from './Styled';

export default {
  title: 'APIReference/Primary/Styled',
  component: Styled,
};

const Template = args => <Styled {...args} />;

export const Default = Template.bind({});
