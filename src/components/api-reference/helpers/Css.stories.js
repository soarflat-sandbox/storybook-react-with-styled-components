import React from 'react';

import { Css } from './Css';

export default {
  title: 'APIReference/Helpers/Css',
  component: Css,
};

const Template = args => <Css {...args} />;

export const Default = Template.bind({});
