import React from 'react';

import { StyleSheetManager1 } from './StyleSheetManager';

export default {
  title: 'APIReference/Helpers/StyleSheetManager',
  component: StyleSheetManager1,
};

const Template = args => <StyleSheetManager1 {...args} />;

export const Default = Template.bind({});
