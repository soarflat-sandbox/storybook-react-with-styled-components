import React from 'react';

import { Keyframes } from './Keyframes';

export default {
  title: 'APIReference/Helpers/Keyframes',
  component: Keyframes,
};

const Template = args => <Keyframes {...args} />;

export const Default = Template.bind({});
