import React from 'react';

import { IsStyledComponent } from './IsStyledComponent';

export default {
  title: 'APIReference/Helpers/IsStyledComponent',
  component: IsStyledComponent,
};

const Template = args => <IsStyledComponent {...args} />;

export const Default = Template.bind({});
