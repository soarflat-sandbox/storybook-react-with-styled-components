import React from 'react';

import { Animations } from './Animations';

export default {
  title: 'Basics/Animations',
  component: Animations,
};

const Template = args => <Animations {...args} />;

export const Default = Template.bind({});
