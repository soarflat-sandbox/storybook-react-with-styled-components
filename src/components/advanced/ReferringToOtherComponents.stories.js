import React from 'react';

import { StyleObjects } from './StyleObjects';

export default {
  title: 'Advanced/StyleObjects',
  component: StyleObjects,
};

const Template = args => <StyleObjects {...args} />;

export const Default = Template.bind({});
