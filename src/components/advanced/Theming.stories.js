import React from 'react';

import { Theming } from './Theming';

export default {
  title: 'Advanced/Theming',
  component: Theming,
};

const Template = args => <Theming {...args} />;

export const Default = Template.bind({});
