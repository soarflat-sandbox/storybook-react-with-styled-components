import React from 'react';

import { Security } from './Security';

export default {
  title: 'Advanced/Security',
  component: Security,
};

const Template = args => <Security {...args} />;

export const Default = Template.bind({});
